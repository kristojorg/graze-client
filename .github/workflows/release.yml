name: Release

on:
  push:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

permissions: {}

jobs:
  release:
    if: github.repository_owner == 'kristojorg'
    name: Release
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write # create the version PR / git tags
      id-token: write # mint the OIDC token for npm trusted publishing
      pull-requests: write # open/update the "Version Packages" PR
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2 # this repo uses bun (bun.lockb)
      - uses: actions/setup-node@v4
        with:
          node-version: "24" # OIDC requires Node >= 22.14.0
      - name: Ensure npm supports OIDC trusted publishing
        run: npm install -g npm@latest # changeset publish shells out to npm; OIDC needs npm >= 11.5.1
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Create Release Pull Request or Publish
        id: changesets
        uses: changesets/action@v1
        with:
          version: bun run changeset-version
          publish: bun run changeset-publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # No NPM_TOKEN — auth comes from the OIDC id-token above.
